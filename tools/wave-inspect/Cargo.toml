[package]
name = "wave-inspect"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Diagnostic CLI for Wave's WAV-to-wavetable pipeline."

[[bin]]
name = "wave-inspect"
path = "src/main.rs"

[dependencies]
superduper-synth-core = { path = "../../synth-core" }
